import { StyleSheet } from 'react-native';

// Calorie Ring Style Sheet

export default StyleSheet.create({
    icons: {
      color: '#f6f8fc',
      position: 'absolute',
      alignSelf: 'center',
  },
});